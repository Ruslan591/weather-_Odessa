{
  "task_id": "1789791741",
  "cmd": "grep -c 'msg_fes' /tmp/caps.xml; grep -c '<Name>' /tmp/caps.xml; grep -o '<Name>[a-zA-Z0-9_:.]*</Name>' /tmp/caps.xml | grep -i 'clm\\|cth\\|ir105\\|geocolour' | head -20",
  "timeout": 30
}