{
  "task_id": "snapshot-1787783091",
  "cmd": "echo === DISK === && df -h / && echo === DU OPT === && du -sh /opt/* 2>/dev/null && echo === DU ETC === && du -sh /etc/vps-agent /etc/vps-github-bridge /etc/caddy 2>/dev/null && echo === MEMORY === && free -h && echo === CRON === && sudo crontab -l 2>/dev/null && echo === SERVICES === && sudo systemctl is-active vps-agent caddy 2>/dev/null && echo === REPO FILES === && ls -la /opt/weather-pipeline/repo | head -15 && echo === BRIDGE LOG TAIL === && tail -20 /var/log/vps-github-bridge.log 2>/dev/null && echo === UPTIME === && uptime",
  "timeout": 30
}