{
  "task_id": "baseline-post-v2-014",
  "cmd": "date -u +'%Y-%m-%dT%H:%M:%SZ' && echo '=== .git size ===' && du -sh /opt/weather-pipeline/repo/.git && echo '=== count-objects ===' && git -C /opt/weather-pipeline/repo count-objects -v -H && echo '=== unreachable --no-reflogs count ===' && git -C /opt/weather-pipeline/repo fsck --unreachable --no-reflogs 2>/dev/null | wc -l && echo '=== marker counts (post-v2-deploy baseline, should be 0 or very low) ===' && for m in UNPUSHED_COMMITS_DETECTED UNPUSHED_COMMIT_RETAINED SYNC_ABORTED_SHALLOW_ERROR GIT_LOCK_TIMEOUT 'push failed after 3 attempts'; do echo \"--- $m ---\"; grep -c \"$m\" /var/log/vps-pipeline.log /var/log/vps-satellite-pipeline.log /var/log/vps-ai-pipeline.log 2>&1; done && echo '=== df -h / ===' && df -h /",
  "timeout": 90
}