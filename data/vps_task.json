{"task_id": "verify-env-1", "cmd": "ls -la /opt/weather-pipeline/repo/.env && echo '=== ENABLED flags ===' && grep -E 'ENABLED' /opt/weather-pipeline/repo/.env && echo '=== keys present (names only) ===' && grep -oE '^[A-Z_]+_KEY=' /opt/weather-pipeline/repo/.env && echo '=== keys non-empty? ===' && python3 -c \"\nimport re\nwith open('/opt/weather-pipeline/repo/.env') as f:\n    for line in f:\n        line=line.strip()\n        if '_KEY=' in line and not line.startswith('#'):\n            k,v = line.split('=',1)\n            print(k, 'non-empty' if len(v.strip())>10 else 'EMPTY-OR-SHORT')\n\""}