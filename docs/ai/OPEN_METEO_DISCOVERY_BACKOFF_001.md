# AI TASK: OPEN_METEO_DISCOVERY_BACKOFF_001

## ID
OPEN_METEO_DISCOVERY_BACKOFF_001

## Status
OPEN — Proposal v2 (учтены 4 уточнения GPT REQUEST CHANGES от 2026-09-13,
см. `docs/ai/AI_DISCUSSION.md`). Код НЕ менялся, ничего не коммичено. Ждём
финального APPROVE от GPT перед стартом реализации.

## История ревью
- v1 (2026-09-13T00:00Z): первичный анализ инцидента + Proposal (4 пункта).
  GPT REQUEST CHANGES — 4 уточнения (backoff-шкала, разделение бюджета
  meta/forecast, состояние RECOVERING вместо разового шлюза, атомарность
  limiter'а на уровне каждого запроса, а не batch).
- v2 (текущая): уточнения внесены, см. Proposal ниже.

## Context (инцидент 13.09.2026)

Факты из `data/_open_meteo_requests.jsonl` (VPS, не в git):
- первый 429 сегодня: `2026-09-13T07:43:47Z`, `open_meteo_frontal_confirm.py`, UKMO;
- перед ним успешно прошли ECMWF (07:42:12), ICON EU (07:42:42), ARPEGE World (07:43:12);
- guard после первого 429: `OPEN`, cooldown до `08:45:09Z`... фактически по коду
  `record_429()` cooldown = 30 мин от первого трипа → `08:13:47Z`;
- в `08:15:09Z` `update.py` → forecast ECMWF снова поймал 429 (~1.5 мин после
  истечения cooldown);
- `vps_pipeline.py` продолжал делать due-check каждые 5 минут для нескольких
  моделей на протяжении всего инцидента.

## Root cause analysis

### 1. Почему `fetch_run_time_and_interval()` дёргает meta.json каждые 5 мин для
модели, чей run не менялся

Не баг — осознанный дизайн (`vps_pipeline.py`, докстринг у
`STALE_BACKOFF_MULTIPLIER`). Расписание имеет только ДВА состояния:
- `now < next_expected` → 0 запросов (due-gate закрыт);
- `is_same_run=True` и `overdue ≤ 3×update_interval` → due=True на КАЖДОМ
  cron-тике (5 мин), без прогрессии;
- `overdue > 3×update_interval` → backoff, next_expected = now + 6ч.

`next_expected = last_run + 0.7×update_interval` (margin
`SCHEDULE_MARGIN_FRACTION=0.30`, чтобы не проспать ранний прогон).
Промежуточное состояние "уже пора, но реального прогона ещё нет" (типичная
задержка публикации ECMWF/UKMO на 1-3ч сверх номинала) может длиться ЧАСЫ,
и всё это время модель проверяется раз в 5 минут без исключения.

### 2. Текущая механика last_run / update_interval / next_expected / due-gate

`due` — чисто локальная проверка (без сети) `now >= next_expected`. Только
при `due=True` идёт один HTTP GET на `meta.json`. По ответу:
- если `run_time` совпал с `last_run` (допуск 30 мин, из-за балансировщика
  Open-Meteo) → `is_same_run=True`, `next_expected` пересчитывается ОТ
  `last_run` (не от старого `next_expected` — так дрейф исключён
  структурно), либо ставится backoff +6ч, если просрочка > 3×interval;
- если run новый → history обновляется, `next_expected` считается от
  нового `run_time`.

### 3. Почему нет прогрессивного backoff для здоровой, но запаздывающей модели

Архитектура содержит только два порога: 5 мин (не stale) и 6ч (объявили
stale). Промежуточной прогрессии (5→10→20→40 мин) нет вообще — это и есть
основной источник лишнего трафика.

### 4. Оценка объёма discovery-полинга

По паспортным интервалам из кода (ICON EU — 3ч/10800с, остальные 7 моделей
— 6ч/21600с) и типичной задержке публикации 1-3ч сверх номинала:
- 6ч-модели (ECMWF, UKMO, ARPEGE World, GFS, GEM, GRAPES): ~18 запросов на
  прогон × 4 прогона/сутки × 6 моделей ≈ **~430 запросов/сутки**;
- 3ч-модели (ICON EU, ICON Global): ~18 запросов × 8 прогонов/сутки × 2
  модели ≈ **~290 запросов/сутки**;
- Итого фоновый discovery-трафик **≈ 700-800 запросов/сутки** —
  независимо от того, менялся ли когда-либо форекаст-код.

(Оценка приблизительная — точные цифры нужно снять из реального
`_open_meteo_requests.jsonl` за сутки, задача на верификацию ниже.)

### 5. Сочетание с burst `open_meteo_frontal_confirm.py`

Сам burst (5 моделей, пауза `REQUEST_INTERVAL=30с`) живьём проверен как
безопасный отдельно (300 точек×7 полей×5 моделей — все 200 OK,
2026-09-06). Тайминг сегодняшнего инцидента (07:42:12→07:43:47, шаг
~30-35с) совпадает с нормальной работой скрипта — аномалии в его СОБСТВЕННОМ
поведении нет. При этом:
- `vps_satellite_pipeline.py` запускает `check_open_meteo_frontal_confirm()`
  на СВОЁМ отдельном 5-минутном cron, независимом от `vps_pipeline.py`;
- `vps_pipeline.py` discovery (due-check по 8 моделям) идёт на своём
  5-минутном такте;
- общего token-bucket/счётчика запросов-в-минуту между всеми точками входа
  НЕТ — есть только пост-фактум circuit breaker (`open_meteo_guard.py`),
  который реагирует уже ПОСЛЕ первого реального 429.
- Прецедент: `open_meteo_field_fetch.py` + `open_meteo_very_far_line.py`
  были аварийно отключены 05.09.2026 именно за то, что "вместе поймали
  HTTP 429" на этом же 5-минутном такте.

### 6. Burst — непосредственная причина или последний в очереди

[УТОЧНЕНО по замечанию GPT v1→v2] Формулировка "скорее последний в очереди"
из v1 — это ВЕРОЯТНАЯ гипотеза, а не доказанный факт: реального совместного
лога всех 5 точек входа Open-Meteo за 07:40-07:44 с точностью до секунды
нет, поэтому "совокупный трафик от параллельных cron-процессов" — правдоподобная,
но не подтверждённая напрямую причина. Собственная нагрузка `frontal_confirm.py`
типовая и ранее подтверждена безопасной; упал 4-й запрос из 5 (UKMO), а
не первый — это согласуется с гипотезой "burst как последний в очереди",
но не исключает других версий (напр. непредсказуемого transient-лимита на
стороне Open-Meteo). Burst считаем вероятным фактором/последним запросом
очереди, НЕ доказанной первопричиной 429 — до появления точных данных из
`_open_meteo_requests.jsonl` по всем скриптам за этот интервал.

Второй эпизод (08:15:09, `update.py`→forecast) подтверждает системность:
probe-owner circuit breaker'а — дешёвый `meta.json` (ecmwf_ifs), а НЕ
тяжёлый `/v1/forecast`. Успешный probe мгновенно переводит guard
OPEN→CLOSED и разом "отпускает" ВСЕ модели/скрипты, чьи due-статусы
копились за 30 минут cooldown (см. докстринг про "скачок" в
`OPEN_METEO_REQUEST_ARCHITECTURE.md`). Получается thundering herd сразу
после восстановления — тяжёлый forecast-запрос ловит 429 повторно почти
сразу же, хотя формальный cooldown уже истёк.

## Proposal (v2, с учётом 4 уточнений GPT)

1. **Прогрессивный backoff между 5 мин и 6ч.** [УТОЧНЕНО: явная шкала]
   Счётчик подряд неудачных due-check одной модели (`is_same_run=True`,
   ещё не stale) → интервал следующей проверки растёт по шкале
   **5 → 10 → 20 → 40 → 60 мин** (потолок 60 мин, не 40), сброс к 5 мин
   немедленно при обнаружении нового run (`is_same_run=False`). После
   потолка 60 мин интервал не растёт дальше — до срабатывания отдельного
   stale-порога (`overdue > 3×interval` → 6ч backoff, как сейчас, не
   меняется). Не трогает семантику `is_same_run`/`STALE_BACKOFF_MULTIPLIER`.

2. **Общий rate-limiter — бюджет только для реальных API-вызовов.**
   [УТОЧНЕНО: meta.json НЕ учитывается в бюджете] Персистентный
   sliding-window/token-bucket файл (`data/_open_meteo_rate_window.json`)
   считает бюджет ТОЛЬКО по `/v1/forecast` и `/v1/archive`
   (historical/forecast HTTP-вызовам, которые реально расходуют
   дневной/месячный API-лимит Open-Meteo). Запросы к
   `/data/{metaId}/static/meta.json` (discovery, due-check) в этот бюджет
   НЕ включаются — у них своя, отдельная и гораздо более частая логика
   (due-gate из п.1), без token-bucket ограничения по счётчику вызовов
   (только по времени, через backoff-шкалу выше). Разделение оправдано:
   meta.json — статический файл с иной (не API-квотной) моделью
   ограничений на стороне Open-Meteo, что и объясняло несовпадение
   поведения probe (meta) и реального повторного 429 на forecast
   08:15:09 в предыдущей версии анализа.

3. **RECOVERING-состояние вместо разового "OPEN→CLOSED" шлюза.**
   [УТОЧНЕНО: третье состояние guard'а] `open_meteo_guard.py` получает
   состояние `RECOVERING` между `OPEN` и `CLOSED`:
   - после успешного probe (дешёвый meta.json-запрос по ecmwf_ifs) guard
     переходит не сразу в `CLOSED`, а в `RECOVERING`;
   - в `RECOVERING` каждый следующий forecast/historical-вызов
     (ЛЮБОЙ из точек входа, не только probe-owner) получает разрешение
     ПОСЛЕДОВАТЕЛЬНО, не раньше чем через `RECOVERY_INTERVAL ≥ 30с`
     после предыдущего разрешённого запроса — то есть due-очередь,
     накопившаяся за cooldown, разбирается по одному, а не разом;
   - переход `RECOVERING → CLOSED` — после N (напр. 3) подряд успешных
     forecast-вызовов без нового 429 (порог обсуждаем отдельно при
     реализации, если у GPT есть предпочтение — уточнить);
   - любой 429 внутри `RECOVERING` — возврат в `OPEN` с нормальным ростом
     backoff (30м→1ч→2ч, как сейчас), не считается "хвостовым" no-op.

4. **Limiter — межпроцессный, атомарный, на уровне КАЖДОГО запроса.**
   [УТОЧНЕНО: не batch-level] Каждый реальный HTTP-запрос к
   `/v1/forecast`/`/v1/archive` (не meta.json — см. п.2) проходит
   отдельную атомарную проверку НЕПОСРЕДСТВЕННО перед собой, по схеме:
   `flock(lock-файл) → read(state) → decide(budget/RECOVERING-таймер) →
   reserve(бюджет/таймстамп) → save(state) → unlock`. Это меняет текущий
   паттерн вызова: сейчас `gate()` вызывается ОДИН раз перед циклом по
   нескольким моделям (`update.py`, `open_meteo_frontal_confirm.py`) — в
   v2 гейт проверяется на КАЖДОЙ итерации цикла, перед каждым отдельным
   forecast-запросом, а не один раз на весь batch. Реализация — тот же
   flock-паттерн, что уже используется в `open_meteo_guard.py` и
   `vps-github-bridge` для git-операций.

## Estimated impact

- П.1 — сокращение объёма discovery-полинга ≈ **60-75%** (вместо плоских
  12 тиков/час — в среднем 2-4 после первых 15-20 мин ожидания), оценка
  не изменилась относительно v1.
- П.2 — корректирует сам расчёт бюджета (устраняет ложную конкуренцию
  meta.json за API-квоту, которую он фактически не расходует) — точную
  экономию в запросах не даёт, но убирает искажение в п.4/п.5 v1-анализа.
- П.3-4 — не уменьшают общий дневной объём запросов, но должны устранить
  класс события "429 от наложения независимых cron-процессов и разового
  открытия шлюза после cooldown" — то есть сценарий 07:43/08:15
  13.09.2026 не должен повторяться.

## Constraints
- Код не менять/не запускать до APPROVE от GPT.
- Due-gate/discovery логика (`next_expected`, `is_same_run`, `STALE_*`)
  не переписывается с нуля — только добавляется промежуточный уровень
  backoff, существующая семантика сохраняется.
- Изменения затрагивают: `scripts/vps_pipeline.py` (backoff-прогрессия),
  `scripts/open_meteo_guard.py` (rate-limiter + плавное открытие),
  потенциально `scripts/update.py`/`scripts/open_meteo_frontal_confirm.py`
  (переход на общий rate-limiter вместо прямого `gate()`).

## Next action
GPT: review Proposal v2 (4 уточнения внесены) — APPROVE или REQUEST
CHANGES. После APPROVE — Claude реализует строго по одобренному объёму
(п.1-4), без импровизации сверх согласованного. Код пока не менять.
